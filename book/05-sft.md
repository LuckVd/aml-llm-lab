# Lab 05 原理：SFT 与 LoRA

> 读顺序建议：本页 → sft.py 的 TODO → 卡住再回来查。
> 基座换成 Qwen2.5-0.5B（lab03 自训的 20M 太小，学不会"对话格式"这种复杂行为）。

## 1. 预训练模型 → 助手，差的是什么？

预训练模型学的是"延续文本"，不是"回答问题"。问它"法国的首都是？"，
它可能接"一个经常被问到的地理问题"——因为语料里这句话后面常跟着这种话。

SFT（supervised fine-tuning）= 拿几千~几万条"指令 → 理想回答"的样本继续训练，
把"延续"行为掰成"听指令"行为。模型的知识没变多少，变的是**行为对齐**。

## 2. 损失只算回答部分

一条训练样本长这样（Qwen 对话格式）：

```
<|im_start|>user
法国的首都是？<|im_end|>
<|im_start|>assistant
巴黎。<|im_end|>
```

**labels 里 prompt 部分全部置 -100**（cross-entropy 的忽略标记），只在
"巴黎。<|im_end|>"上算损失。若不遮罩，模型还要学"复述用户问题"这个无用任务，
有效信号被稀释，回答质量明显下降——这是 SFT 最重要的一个细节。

## 3. LoRA：不动大矩阵，只学"增量"

全参微调 0.5B 模型要存 2GB 参数 + 2GB 梯度 + 4GB Adam 状态。LoRA 的观察：
微调时的权重变化量 ΔW 是**低秩**的——信息量远小于参数量。那就把 ΔW 分解：

```
W' = W + B·A        A: (r, d_in),  B: (d_out, r),  r << d
输出 = x·Wᵀ + (x·Aᵀ)·Bᵀ · α/r
```

- 原矩阵 W **冻结**；只训 A、B（r=16 时约为全参数的 1–5%）
- A 高斯初始化、B 零初始化（训练起点 ΔW=0，不破坏原模型）
- α/r 缩放：控制增量相对原矩阵的"音量"
- 收工时可以把 BA 合并回 W，推理零开销

只挂在注意力的 q/v（原论文）或全部投影（现代做法）上都行；挂在 MLP 上往往收益更大。

## 4. 训练循环：和 lab03 是同一件事

AdamW + warmup/cosine + grad clip，一套参数走天下。差别只有三处：

1. batch 来自 `collate`（变长 → pad + attention_mask + labels=-100）
2. 只有 LoRA 参数进优化器：`AdamW([p for p in model.parameters() if p.requires_grad])`
3. 学习率更小（~1e-4 量级，LoRA 常用）且轮数少（几 epoch 就够，SFT 过拟合很快）

## 5. 评估：SFT 的坑在于"loss 降了，回答反而变差"

SFT loss 只能监控"学指令格式"学得好不好；回答质量要用采样人审/规则判分（lab08 的 self-judge 会回来收拾这里）。观察点：
- 训完的模型应稳定输出 `<|im_end|>` 结尾、不再复读问题
- 过训练的标志：回答越来越短、越来越同质化（mode collapse 的雏形）

## 6. 带着这些问题去写 TODO

1. labels 若不遮罩 prompt，损失的"有效 token 数"占比大约变成多少？（用你的数据算一下）
2. LoRA 的 B 为什么必须零初始化，A 不能零初始化？
3. r=4 / 16 / 64 各训一次，可训练参数量差多少？回答质量差多少？
4. 为什么 LoRA 的 lr（1e-4）比预训练（6e-4）小，却比"全参微调小模型"常见值大？
5. 0.5B 模型 SFT 后能学会"计算 3+4"吗？哪类指令能学会、哪类永远学不会？（lab07 用 RLVR 教算术，正是补这个）

写完 pytest 全绿后跑 `demo_sft.py`（离线小模型演示 LoRA 记住四条指令），下载 Qwen 后跑 `uv run python labs/05-sft/sft.py` 做真训练。
